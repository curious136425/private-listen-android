<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <receiver
            android:name=".QaControlReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP">
            <intent-filter>
                <action android:name="com.privatelisten.app.qa.action.SIMULATE_AUDIO_NOISY" />
                <action android:name="com.privatelisten.app.qa.action.SIMULATE_AUDIO_CAN_DUCK" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
